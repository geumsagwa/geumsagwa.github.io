# 37. 코드쉴드 적용결과 (homepage, 2026-03-14)

## 적용 범위

- 대상: `project/homepage`
- 기준: `project/code-shield/36_타프로젝트_적용_30분_체크리스트_v1.md`

## 반영 항목

- 로컬 강제:
  - `.pre-commit-config.yaml` 추가
- 원격 강제:
  - `.github/workflows/quality-gate.yml` 추가
  - `.github/workflows/weekly-quality-report.yml` 추가
  - `.github/pull_request_template.md` 추가
- AST 규칙:
  - `.semgrep/rules.yml` 추가
- 운영 스크립트:
  - `scripts/weekly-quality-report.ps1` 추가
  - `scripts/branch-protection.ps1` 추가
- 실행 스크립트:
  - `package.json`에 `verify:onboarding`, `branch-protection:*`, `report:weekly` 추가
- 환경 표준:
  - `.editorconfig`, `.prettierrc.json`, `.prettierignore` 추가
  - `.gitignore`에 `coverage/`, `reports/weekly-quality-*.md` 추가

## 검증 결과

- `npm run check`: PASS
- `npm run verify:onboarding`: PASS
- `npm run report:weekly`: PASS (리포트 생성)
- `npm run branch-protection:check`: PASS (`geumsagwa/geumsagwa.github.io/master`)

## 추가 보강

- `scripts/branch-protection.ps1`가 기본 브랜치를 자동 감지하도록 개선
  - 원격 저장소의 default branch 조회 후 해당 브랜치 기준으로 보호 상태 확인

## 후속 작업

1. 로컬에서 pre-commit 설치:
   - `pre-commit install --config .pre-commit-config.yaml`
2. 운영 저장소 연결 후 branch protection 적용:
   - `npm run branch-protection:check`
   - `npm run branch-protection:apply`
